<Page x:Class="DcsTranslationTool.Presentation.Wpf.Features.TranslationFileSelection.TranslationFileSelectionView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:resx="clr-namespace:DcsTranslationTool.Resources;assembly=DcsTranslationTool.Resources"
      xmlns:local="clr-namespace:DcsTranslationTool.Presentation.Wpf.Features.TranslationFileSelection"
      xmlns:common="clr-namespace:DcsTranslationTool.Presentation.Wpf.Features.Common"
      xmlns:vm="clr-namespace:DcsTranslationTool.Presentation.Wpf.ViewModels"
      xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:cal="http://www.caliburnproject.org"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance Type=local:TranslationFileSelectionViewModel}"
      d:DesignHeight="450"
      d:DesignWidth="800">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Style="{StaticResource MaterialDesignDisplayLargeTextBlock}"
                   Text="{x:Static resx:Strings_Translation.Title}" />

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TabControl Grid.Row="0"
                        Style="{StaticResource MaterialDesignFilledTabControl}"
                        Margin="{StaticResource SmallTopMargin}"
                        SelectedIndex="{Binding SelectedTabIndex, Mode=TwoWay}"
                        ItemsSource="{Binding Tabs}">
                <TabControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:TabItemViewModel}">
                        <TextBlock Text="{Binding Title}" />
                    </DataTemplate>
                </TabControl.ItemTemplate>

                <TabControl.ContentTemplate>
                    <DataTemplate DataType="{x:Type vm:TabItemViewModel}">
                        <Grid>
                            <TreeView x:Name="TreeViewControl"
                                      Style="{StaticResource CommonFileTreeViewStyle}"
                                      ItemsSource="{Binding Root.Children}"
                                      Margin="{StaticResource XSmallTopMargin}"
                                      VirtualizingStackPanel.IsVirtualizing="True"
                                      VirtualizingStackPanel.VirtualizationMode="Recycling"
                                      ScrollViewer.CanContentScroll="True">
                                <TreeView.ItemContainerStyle>
                                    <StaticResource ResourceKey="CommonFileTreeViewItemStyle" />
                                </TreeView.ItemContainerStyle>
                                <TreeView.ItemTemplate>
                                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                        <StackPanel Orientation="Horizontal">
                                            <md:PackIcon VerticalAlignment="Center">
                                                <md:PackIcon.Style>
                                                    <Style TargetType="md:PackIcon">
                                                        <Setter Property="Kind" Value="FolderZipOutline" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsDirectory}" Value="True">
                                                                <Setter Property="Kind" Value="FolderOutline" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </md:PackIcon.Style>
                                            </md:PackIcon>
                                            <TextBlock Text="{Binding Name}"
                                                       Margin="{StaticResource XSmallLeftMargin}"
                                                       VerticalAlignment="Center" />
                                        </StackPanel>
                                    </HierarchicalDataTemplate>
                                </TreeView.ItemTemplate>
                            </TreeView>

                            <TextBlock Text="{Binding DataContext.CurrentStatusMessage, RelativeSource={RelativeSource AncestorType=Page}}"
                                       Visibility="{Binding DataContext.HasCurrentStatusMessage, RelativeSource={RelativeSource AncestorType=Page}, Converter={StaticResource BoolToVisibilityConverter}}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       TextWrapping="Wrap"
                                       TextAlignment="Center"
                                       Foreground="{DynamicResource MaterialDesignBodySecondary}"
                                       Margin="{StaticResource MediumXMargin}" />
                        </Grid>
                    </DataTemplate>
                </TabControl.ContentTemplate>
            </TabControl>

            <StackPanel Grid.Row="1"
                        Orientation="Horizontal"
                        Margin="{StaticResource MediumTopMargin}">
                <common:ActionButton IconKind="Reload"
                                     Text="{x:Static resx:Strings_Translation.RefreshButtonContent}"
                                     IsEnabled="{Binding CanRefresh}"
                                     cal:Message.Attach="[Event Click] = [Action Refresh]" />
                <common:ActionButton IconKind="FolderOpenOutline"
                                     Margin="{StaticResource SmallLeftMargin}"
                                     Text="{x:Static resx:Strings_Translation.OpenDirectoryButtonContent}"
                                     IsEnabled="{Binding CanOpenDirectory}"
                                     cal:Message.Attach="[Event Click] = [Action OpenDirectory]" />
                <common:ActionButton IconKind="Translate"
                                     Margin="{StaticResource SmallLeftMargin}"
                                     Text="{x:Static resx:Strings_Translation.CreateTranslationButtonContent}"
                                     IsEnabled="{Binding CanCreateTranslation}"
                                     cal:Message.Attach="[Event Click] = [Action CreateTranslation]" />
            </StackPanel>
        </Grid>
    </Grid>

</Page>
