<Page x:Class="DcsTranslationTool.Presentation.Wpf.Features.Main.MainView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:resx="clr-namespace:DcsTranslationTool.Resources;assembly=DcsTranslationTool.Resources"
      xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:cal="http://www.caliburnproject.org"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid x:Name="ButtonsGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Button Style="{StaticResource MainPageSelectButtonStyle}"
                Height="Auto"
                MinHeight="300"
                Tag="{x:Static md:PackIconKind.Download}"
                Content="{x:Static resx:Strings_Download.Title}"
                cal:Message.Attach="NavToDownload()">
        </Button>

        <Button Grid.Column="1"
                Style="{StaticResource MainPageSelectButtonStyle}"
                Height="Auto"
                MinHeight="300"
                Tag="{x:Static md:PackIconKind.Upload}"
                Content="{x:Static resx:Strings_Upload.Title}"
                cal:Message.Attach="NavToUpload()">
        </Button>

        <Button Grid.Column="2"
                Style="{StaticResource MainPageSelectButtonStyle}"
                Height="Auto"
                MinHeight="300"
                Tag="{x:Static md:PackIconKind.Translate}"
                Content="{x:Static resx:Strings_Translation.Title}"
                cal:Message.Attach="NavToTranslationFileSelection()">
        </Button>
    </Grid>

</Page>
