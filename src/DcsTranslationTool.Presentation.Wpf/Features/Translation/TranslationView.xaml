<Page x:Class="DcsTranslationTool.Presentation.Wpf.Features.Translation.TranslationView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:resx="clr-namespace:DcsTranslationTool.Resources;assembly=DcsTranslationTool.Resources"
      xmlns:local="clr-namespace:DcsTranslationTool.Presentation.Wpf.Features.Translation"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance Type=local:TranslationViewModel}"
      d:DesignHeight="450"
      d:DesignWidth="800">

    <Grid Margin="{StaticResource SmallXMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Margin="{StaticResource MediumBottomMargin}"
                   Style="{StaticResource MaterialDesignDisplayLargeTextBlock}"
                   Text="{x:Static resx:Strings_Translation.Title}" />
    </Grid>

</Page>
